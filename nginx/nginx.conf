events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name _;
        
        # In a real setup, redirect 80 to 443. For test, simply proxy.
        location / {
            proxy_pass http://app:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
